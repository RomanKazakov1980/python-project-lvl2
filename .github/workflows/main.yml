name: run-linter

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check repository code
        uses: actions/checkout@v3
      - name: Install dependencies
        run:
          pip install poetry
          make install
      - name: Run linter
        run: |
          make lint